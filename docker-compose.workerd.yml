# Use this for running the Cloudflare Worker locally with workerd runtime
services:
  worker:
    build:
      context: .
      dockerfile: Dockerfile.workerd
    ports:
      - "8080:8080"
    environment:
      - ENVIRONMENT=local
